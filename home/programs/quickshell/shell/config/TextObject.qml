import QtQuick
import QtQuick.Layouts

Text {
    font: Qt.font({
        // font HAS to have cjk characters in it, or any text with cjk chars gets misaligned
        family: "UDEVGothic35NFLG",
        pointSize: 13
    })
}
